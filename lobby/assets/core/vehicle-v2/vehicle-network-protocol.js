export const VEHICLE_NETWORK_PROTOCOL_VERSION=2;
const finite=(value,fallback=0)=>Number.isFinite(Number(value))?Number(value):fallback;
export function vehicleNetworkPacket(snapshot={},extra={}){return Object.freeze({v:VEHICLE_NETWORK_PROTOCOL_VERSION,id:String(snapshot.id||''),worldId:String(snapshot.worldId||extra.worldId||''),kind:String(snapshot.kind||'utility'),mobilityType:snapshot.mobilityType==='aerial'?'aerial':'ground',x:finite(snapshot.x??snapshot.pose?.x),y:finite(snapshot.y??snapshot.pose?.y),z:finite(snapshot.z??snapshot.pose?.z),heading:finite(snapshot.heading??snapshot.pose?.heading),speedKmh:finite(snapshot.speedKmh),verticalSpeed:finite(snapshot.verticalSpeed),phase:String(snapshot.phase||'parked'),driverId:snapshot.driverId||extra.driverId||null,seq:Math.max(0,Math.floor(finite(extra.seq))),t:finite(extra.t,Date.now())});}
export function interpolateVehiclePacket(a,b,alpha=.5){alpha=Math.max(0,Math.min(1,Number(alpha)||0));if(!a)return b;if(!b)return a;let dh=finite(b.heading)-finite(a.heading);while(dh>Math.PI)dh-=Math.PI*2;while(dh<-Math.PI)dh+=Math.PI*2;const lerp=(x,y)=>finite(x)+(finite(y)-finite(x))*alpha;return{...b,x:lerp(a.x,b.x),y:lerp(a.y,b.y),z:lerp(a.z,b.z),heading:finite(a.heading)+dh*alpha,speedKmh:lerp(a.speedKmh,b.speedKmh),verticalSpeed:lerp(a.verticalSpeed,b.verticalSpeed)};}
