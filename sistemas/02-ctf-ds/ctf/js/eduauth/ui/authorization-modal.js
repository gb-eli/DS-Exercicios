import { escapeHtml } from '../../core/utils.js';
import { EDUAUTH_PLATFORM } from '../config/platform.js';
import { EDUAUTH_KEY_CONFIG } from '../config/key-config.js';
import { registryLabelFor } from '../config/registries.js';

const riskLabel = { LOW: 'Baixo', MEDIUM: 'Médio', HIGH: 'Alto', CRITICAL: 'Crítico' };
const modeLabel = { CLASS_SHARED_PIN: 'PIN coletivo da turma', SESSION_SCOPED_PIN: 'PIN individual da sessão', SIGNED_GRANT: 'Autorização assinada', PROFILE_RECOVERY_ENVELOPE: 'Recuperação criptográfica' };
export const renderAuthorizationModal = ({ action, request = null, profile = null, resourceId = '', allowDevelopmentBypass = false }) => {
  const context = request?.context;
  const signed = action.preferredMode === 'SIGNED_GRANT';
  const expires = request?.expiresAt ? Math.max(0, Math.ceil((request.expiresAt - Date.now()) / 1000)) : action.ttlSeconds;
  return `<div class="modal-layer eduauth-layer" role="presentation"><section class="panel eduauth-modal" role="dialog" aria-modal="true" aria-labelledby="eduauth-title">
    <header><div><p class="eyebrow">EDUAUTH OFFLINE // AUTORIZAÇÃO CONTEXTUAL</p><h2 id="eduauth-title">${escapeHtml(action.label)}</h2></div><button class="icon-button" data-close-modal aria-label="Fechar">×</button></header>
    <div class="eduauth-environment ${EDUAUTH_PLATFORM.productionProvisioned ? 'production' : 'development'}"><span>${EDUAUTH_PLATFORM.productionProvisioned ? '✓' : '⚠'}</span><div><strong>${EDUAUTH_PLATFORM.productionProvisioned ? 'Provisionamento de produção ativo' : 'Integração estrutural de desenvolvimento'}</strong><small>${EDUAUTH_PLATFORM.productionProvisioned ? 'A autorização será exigida sem bypass.' : 'Chaves marcadas como teste. Não utilize estes códigos em atividade real.'}</small></div></div>
    <div class="eduauth-context-grid"><div><span>RISCO</span><b>${riskLabel[action.risk] || action.risk}</b></div><div><span>MODALIDADE</span><b>${modeLabel[action.preferredMode] || action.preferredMode}</b></div><div><span>TURMA</span><b>${escapeHtml(profile?.className || registryLabelFor('classes', context?.classCode || 0))}</b></div><div><span>RECURSO</span><b>${escapeHtml(resourceId || 'plataforma')}</b></div></div>
    ${action.requireReason ? `<label class="eduauth-reason">Motivo da autorização<textarea data-eduauth-reason rows="2" minlength="4" maxlength="300" required placeholder="Registre por que esta autorização é necessária"></textarea></label>` : ''}
    ${signed ? `<p>Esta ação exige um token completo assinado pelo EduAuth Professor. A plataforma possui somente a chave pública de verificação.</p><div class="eduauth-request"><span>SOLICITAÇÃO PÚBLICA PARA O PROFESSOR</span><code data-eduauth-code>${escapeHtml(request?.code || '')}</code><div class="eduauth-code-actions"><button class="secondary-button compact" data-copy-eduauth-code type="button">COPIAR</button><button class="secondary-button compact" data-speak-eduauth-code type="button">OUVIR</button><button class="secondary-button compact" data-toggle-eduauth-qr type="button">QR OPCIONAL</button></div><canvas class="eduauth-qr hidden" data-eduauth-qr aria-label="QR Code da solicitação"></canvas></div><form data-eduauth-signed-form><input type="hidden" name="actionId" value="${escapeHtml(action.id)}"><input type="hidden" name="resourceId" value="${escapeHtml(resourceId)}"><label>Autorização assinada<textarea name="token" rows="6" required placeholder="EA1-G1.payload.assinatura"></textarea></label><div class="eduauth-live-row"><span>Validade da solicitação</span><b data-eduauth-countdown data-expires-at="${request?.expiresAt || 0}">${expires}s</b><span>Transferência</span><b>COLAR / QR / ARQUIVO</b></div><button class="primary-button" type="submit">VERIFICAR AUTORIZAÇÃO</button><button class="secondary-button" data-renew-eduauth-request type="button">GERAR NOVA SOLICITAÇÃO</button></form>` : `
      <div class="eduauth-request"><span>CÓDIGO-BASE PÚBLICO</span><code data-eduauth-code>${escapeHtml(request?.code || '')}</code><div class="eduauth-code-actions"><button class="secondary-button compact" data-copy-eduauth-code type="button">COPIAR</button><button class="secondary-button compact" data-speak-eduauth-code type="button">OUVIR</button><button class="secondary-button compact" data-toggle-eduauth-qr type="button">QR OPCIONAL</button></div><canvas class="eduauth-qr hidden" data-eduauth-qr aria-label="QR Code do código-base"></canvas></div>
      <form data-eduauth-pin-form><input type="hidden" name="actionId" value="${escapeHtml(action.id)}"><input type="hidden" name="resourceId" value="${escapeHtml(resourceId)}"><input type="hidden" name="requestIdTag" value="${escapeHtml(context?.requestIdTag || '')}"><label>Senha informada pelo professor<input name="pin" inputmode="numeric" autocomplete="one-time-code" maxlength="13" required placeholder="${action.pinLength === 10 ? '0000 0000 00' : '0000 0000'}"></label><div class="eduauth-live-row"><span>Validade</span><b data-eduauth-countdown data-expires-at="${request?.expiresAt || 0}">${expires}s</b><span>Tentativas</span><b data-eduauth-attempts>0/5</b></div><button class="primary-button" type="submit">VALIDAR CÓDIGO</button><button class="secondary-button" data-renew-eduauth-request type="button">GERAR NOVA SOLICITAÇÃO</button></form>`}
    <div class="eduauth-feedback" data-eduauth-feedback role="status">A senha nunca é exibida nem registrada pela plataforma.</div>
    ${allowDevelopmentBypass ? `<button class="text-button eduauth-dev-bypass" data-eduauth-development-bypass type="button">CONTINUAR SOMENTE NESTE AMBIENTE DE DESENVOLVIMENTO</button>` : ''}
    <p class="safety-note">Proteção operacional offline. Em um frontend público, material simétrico pode ser analisado por usuário avançado; ações críticas devem usar autorização assinada.</p>
  </section></div>`;
};
