import { APP_VERSION, CLASSES, AREAS, LESSONS } from './data.js?v=20260811r38';
import { getSettings } from './storage.js?v=20260811r38';
import { decryptObject,downloadJSON,downloadBlob,verifyTeacherVaultCredential } from './crypto.js?v=20260811r38';
import { downloadResultPDF } from './result-pdf.js?v=20260811r38';
import { unlockTeacherVault,getTeacherRecords,saveTeacherRecords,clearTeacherRecords,lockTeacherVault } from './teacher-store.js?v=20260811r38';
import { getTeacherCredentialStatus } from './teacher-auth.js?v=20260811r38';
import { sanitizeDiagnosticResult,safeFileName,cleanText } from './security.js?v=20260811r38';
import { lessonCodePanelHTML, mountLessonCodePanel, unmountLessonCodePanel } from './teacher-codes.js?v=20260811r38';
import { installModalManager } from './modal-manager.js?v=20260811r38';

installModalManager();
const root=document.querySelector('#teacherApp');const toastHost=document.querySelector('#toastHost');
let records=[],unlocked=false,credentialStatus=await getTeacherCredentialStatus();
const TEACHER_IDLE_LIMIT_MS=12*60*1000;let teacherLastActivity=Date.now();let teacherIdleTimer=null;
const ATTEMPT_KEY='agv.teacher.failedAttempts.v2',LOCK_KEY='agv.teacher.lockUntil.v2';
applyTeacherTheme();
render();


function applyTeacherTheme(){const settings=getSettings();const requested=settings.theme||'dark';const resolved=requested==='system'?(matchMedia('(prefers-color-scheme: light)').matches?'light':'dark'):requested;document.body.classList.remove('theme-dark','theme-light');document.body.classList.add(`theme-${resolved}`);document.body.classList.toggle('high-contrast',Boolean(settings.highContrast));document.body.classList.toggle('reading-mode',Boolean(settings.readingMode));document.body.classList.toggle('reduced-motion',Boolean(settings.reducedMotion));document.documentElement.style.setProperty('--font-scale',settings.fontScale||1)}

function esc(v=''){return String(v).replace(/[&<>"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]))}
function toast(message,type='good'){const el=document.createElement('div');el.className=`toast ${type}`;el.textContent=message;toastHost.appendChild(el);setTimeout(()=>el.remove(),3500)}
function render(){if(!unlocked)return renderLogin();records=dedupe(getTeacherRecords());renderDashboard()}
function failedAttempts(){return Math.max(0,Number(sessionStorage.getItem(ATTEMPT_KEY)||0))}
function lockRemaining(){return Math.max(0,Number(sessionStorage.getItem(LOCK_KEY)||0)-Date.now())}
function registerFailedAttempt(){const count=failedAttempts()+1;sessionStorage.setItem(ATTEMPT_KEY,String(count));if(count>=5){const blocks=Math.floor(count/5);sessionStorage.setItem(LOCK_KEY,String(Date.now()+Math.min(5,blocks)*60000))}return count}
function clearFailedAttempts(){sessionStorage.removeItem(ATTEMPT_KEY);sessionStorage.removeItem(LOCK_KEY)}
function markTeacherActivity(){if(unlocked)teacherLastActivity=Date.now()}
function startTeacherIdleLock(){clearInterval(teacherIdleTimer);teacherLastActivity=Date.now();teacherIdleTimer=setInterval(()=>{if(unlocked&&Date.now()-teacherLastActivity>=TEACHER_IDLE_LIMIT_MS)lockTeacherSession('Painel bloqueado após 12 minutos sem atividade.')},15000)}
function lockTeacherSession(message='Sessão encerrada com segurança.'){clearInterval(teacherIdleTimer);teacherIdleTimer=null;unmountLessonCodePanel();lockTeacherVault();unlocked=false;records=[];render();toast(message,'warn')}
['pointerdown','keydown','touchstart','scroll'].forEach(event=>document.addEventListener(event,markTeacherActivity,{passive:true}));
document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible'&&unlocked&&Date.now()-teacherLastActivity>=TEACHER_IDLE_LIMIT_MS)lockTeacherSession('Painel bloqueado por inatividade.')});

function renderLogin(){
  unmountLessonCodePanel();
  const header=`<header class="topbar teacher-login-topbar"><div class="brand"><div class="brand-mark"><span>AGV</span><i></i></div><div><h1>Área restrita</h1><p>Painel do Professor</p></div></div><a class="btn btn-secondary btn-small" href="index.html">← Voltar à plataforma</a></header>`;
  if(!credentialStatus.available){root.innerHTML=header+`<section class="panel panel-pad teacher-access-card"><span class="hero-kicker">NAVEGADOR INCOMPATÍVEL</span><h2>Não foi possível abrir o acesso protegido</h2><p>Utilize Chrome, Edge ou Firefox atualizados, fora do modo privado, e permita o armazenamento do site.</p></section>`;return}
  const remaining=lockRemaining();const blocked=remaining>0;
  root.innerHTML=header+`<section class="teacher-login-shell"><div class="panel teacher-access-card"><div class="teacher-access-visual" aria-hidden="true"><div class="teacher-shield">⌾</div><span>ACESSO PROTEGIDO</span><h2>Controle da aula e dos resultados</h2><p>As senhas das aulas, importações e relatórios ficam disponíveis somente após a autenticação do professor.</p><div class="teacher-security-points"><span>✓ Senha mestre da publicação</span><span>✓ Cofre local criptografado</span><span>✓ Bloqueio automático por inatividade</span></div></div><div class="teacher-access-form"><span class="hero-kicker">PAINEL DO PROFESSOR</span><h2>Digite a senha de acesso</h2><p>Esta senha não pode ser criada ou redefinida por alunos neste navegador.</p><form id="teacherLoginForm" class="stack"><div class="field"><label for="teacherCode">Senha mestre</label><div class="password-field"><input class="input" id="teacherCode" type="password" autocomplete="current-password" spellcheck="false" ${blocked?'disabled':''}><button type="button" class="password-toggle" id="toggleTeacherPassword" aria-label="Mostrar senha">◉</button></div><span class="helper" id="capsNotice">Dica: ${esc(credentialStatus.hint||'senha administrativa atual')}. O acesso é bloqueado após tentativas repetidas e encerrado após 12 minutos sem atividade.</span></div><button class="btn btn-primary teacher-login-button" id="unlock" type="submit" ${blocked?'disabled':''}>${blocked?'Acesso temporariamente bloqueado':'Entrar no painel'}</button><div class="teacher-login-status ${blocked?'bad':''}" id="teacherLoginStatus" role="status">${blocked?`Tente novamente em ${Math.ceil(remaining/1000)} segundos.`:'Use a senha mestre definida para esta publicação.'}</div></form><div class="teacher-static-warning"><strong>Limitação do GitHub Pages</strong><span>Esta barreira impede acesso casual. Como o projeto é estático, proteção equivalente a conta institucional exige um servidor de autenticação.</span></div></div></div></section>`;
  const input=root.querySelector('#teacherCode'),form=root.querySelector('#teacherLoginForm'),status=root.querySelector('#teacherLoginStatus'),toggle=root.querySelector('#toggleTeacherPassword'),button=root.querySelector('#unlock'),caps=root.querySelector('#capsNotice');
  toggle.onclick=()=>{const showing=input.type==='text';input.type=showing?'password':'text';toggle.textContent=showing?'◉':'◌';toggle.setAttribute('aria-label',showing?'Mostrar senha':'Ocultar senha');input.focus()};
  input.addEventListener('keyup',event=>{caps.textContent=event.getModifierState?.('CapsLock')?'Atenção: Caps Lock está ativado.':`Dica: ${credentialStatus.hint||'senha administrativa atual'}. O acesso é encerrado após 12 minutos sem atividade.`});
  if(blocked){const timer=setInterval(()=>{const left=lockRemaining();if(!document.body.contains(status)){clearInterval(timer);return}if(left<=0){clearInterval(timer);render();return}status.textContent=`Tente novamente em ${Math.ceil(left/1000)} segundos.`},1000);return}
  input.focus();
  form.onsubmit=async event=>{event.preventDefault();if(lockRemaining()>0){render();return}button.disabled=true;button.textContent='Verificando...';status.className='teacher-login-status';status.textContent='Validando a senha mestre e abrindo o cofre local.';const password=input.value;
    if(!await verifyTeacherVaultCredential(password)){const attempts=registerFailedAttempt();input.value='';input.focus();status.className='teacher-login-status bad';status.textContent=attempts>=5?'Muitas tentativas. O acesso foi bloqueado temporariamente.':`Senha incorreta. Tentativa ${attempts} de 5 antes do bloqueio.`;toast('Senha do professor incorreta.','bad');if(lockRemaining()>0)render();else{button.disabled=false;button.textContent='Entrar no painel'}return}
    try{await unlockTeacherVault(password);clearFailedAttempts();unlocked=true;records=dedupe(getTeacherRecords());input.value='';startTeacherIdleLock();render();toast('Painel do professor desbloqueado.','good')}catch(err){status.className='teacher-login-status bad';status.textContent='A senha foi validada, mas o cofre local não pôde ser aberto. Limpe os dados antigos deste site ou use o navegador da publicação atual.';button.disabled=false;button.textContent='Entrar no painel';toast(err.message,'bad')}
  };
}
function renderDashboard(){
  unmountLessonCodePanel();
  root.innerHTML=`<header class="topbar teacher-dashboard-topbar"><div class="brand"><div class="brand-mark"><span>AGV</span><i></i></div><div><h1>Painel do Professor</h1><p>Controle pedagógico · versão ${APP_VERSION}</p></div></div><div class="top-actions"><span class="teacher-session-chip">● Sessão protegida</span><a class="btn btn-secondary btn-small" href="index.html">Plataforma</a><button class="btn btn-secondary btn-small" id="lock">Bloquear painel</button></div></header><section class="teacher-dashboard-hero panel"><div><span class="hero-kicker">CENTRAL DA AULA</span><h2>Senhas, acompanhamento e registros em um só lugar</h2><p>Selecione a aula, compartilhe a senha atual e organize os comprovantes recebidos dos estudantes.</p></div><div class="teacher-dashboard-metrics"><article><small>Acesso</small><strong>Protegido</strong><span>Senha mestre</span></article><article><small>Bloqueio automático</small><strong>12 min</strong><span>Sem atividade</span></article><article><small>Resultados salvos</small><strong>${records.length}</strong><span>Neste navegador</span></article></div></section>${lessonCodePanelHTML({lessons:LESSONS,classes:CLASSES,esc})}<div class="teacher-workspace"><section class="panel panel-pad teacher-import-panel"><div class="section-title" style="margin-top:0"><div><span class="section-kicker">REGISTROS</span><h2>Importar resultados</h2><p>Receba arquivos <code>.agvresult</code> ou extratos JSON exportados pelos estudantes.</p></div></div><label class="dropzone teacher-dropzone" id="dropzone"><span class="dropzone-icon">⇧</span><strong>Arraste os arquivos para esta área</strong><p>Ou selecione um ou vários arquivos. Os dados permanecem neste navegador.</p><input type="file" id="files" multiple accept=".agvresult,.json,application/json,application/octet-stream" hidden><span class="btn btn-primary btn-small">Selecionar arquivos</span></label></section><section class="panel panel-pad teacher-guidance-panel"><span class="section-kicker">ROTINA RECOMENDADA</span><h2>Fluxo rápido em sala</h2><ol class="teacher-checklist"><li><b>1</b><span>Escolha a turma e a aula no gerador.</span></li><li><b>2</b><span>Projete ou informe a senha atual.</span></li><li><b>3</b><span>Ao final, peça o comprovante em PDF.</span></li><li><b>4</b><span>Importe os resultados quando necessário.</span></li></ol><div class="info-card success"><h4>Questões revisadas</h4><p>As posições das respostas são equilibradas e as alternativas das Aulas 1 e 2 foram reescritas para eliminar pistas pelo tamanho do texto.</p></div></section></div><section class="panel panel-pad teacher-results-panel"><div class="section-title" style="margin-top:0"><div><span class="section-kicker">ANÁLISE</span><h2>Resultados (${records.length})</h2><p>Filtre, confira integridade e exporte os registros necessários.</p></div><div class="action-cluster"><button class="btn btn-secondary btn-small" id="exportCsv">Exportar CSV</button><button class="btn btn-secondary btn-small" id="exportJson">Exportar JSON</button><button class="btn btn-danger btn-small" id="clear">Limpar painel</button></div></div><div class="teacher-filter-bar"><div class="field"><label>Modalidade</label><select class="select" id="filterMode"><option value="">Todas</option><option value="general">Diagnóstico geral</option><option value="class">Diagnóstico da turma</option></select></div><div class="field"><label>Turma</label><select class="select" id="filterClass"><option value="">Todas</option><option value="1ADM">1º ADM</option><option value="2ADM">2º ADM</option></select></div><div class="field"><label>Buscar estudante</label><input class="input" id="filterName" placeholder="Digite o nome"></div><div class="field"><label>Integridade mínima</label><select class="select" id="filterIntegrity"><option value="0">Qualquer</option><option value="70">70%</option><option value="85">85%</option><option value="95">95%</option></select></div></div><div id="tableHost" style="margin-top:16px"></div></section>`;
  root.querySelector('#lock').onclick=()=>lockTeacherSession();const input=root.querySelector('#files'),drop=root.querySelector('#dropzone');input.onchange=e=>importFiles([...e.target.files]);drop.ondragover=e=>{e.preventDefault();drop.classList.add('dragging')};drop.ondragleave=()=>drop.classList.remove('dragging');drop.ondrop=e=>{e.preventDefault();drop.classList.remove('dragging');importFiles([...e.dataTransfer.files])};['filterMode','filterClass','filterName','filterIntegrity'].forEach(id=>root.querySelector('#'+id).oninput=renderTable);root.querySelector('#exportCsv').onclick=exportCSV;root.querySelector('#exportJson').onclick=()=>downloadJSON(records,'resultados-agv.json');root.querySelector('#clear').onclick=async()=>{if(confirm('Limpar todos os resultados deste painel?')){await clearTeacherRecords();records=[];renderDashboard();toast('Cofre local limpo.','good')}};renderTable();mountLessonCodePanel(root,{lessons:LESSONS,classes:CLASSES,esc,toast}).catch(error=>toast(error.message,'bad'))
}
function renderTable(){const host=root.querySelector('#tableHost');if(!host)return;const mode=root.querySelector('#filterMode').value,cls=root.querySelector('#filterClass').value,name=root.querySelector('#filterName').value.toLowerCase(),min=Number(root.querySelector('#filterIntegrity').value);const list=records.filter(r=>(!mode||r.diagnosticMode===mode)&&(!cls||r.selectedClass===cls)&&(!name||r.student?.name?.toLowerCase().includes(name))&&((r.integrity?.score??100)>=min));if(!list.length){host.innerHTML='<div class="empty-state">Nenhum resultado corresponde aos filtros.</div>';return}host.innerHTML=`<div class="teacher-table-wrap"><table class="teacher-table"><thead><tr><th>Estudante</th><th>Turma</th><th>Modalidade</th><th>Proficiência</th><th>Classificação</th><th>Validade</th><th>Acertos</th><th>Tempo</th><th>Integridade</th><th>Data</th><th></th></tr></thead><tbody>${list.map(r=>`<tr><td><strong>${esc(r.student?.name)}</strong></td><td>${CLASSES[r.selectedClass]?.short||esc(r.selectedClass)}</td><td>${r.diagnosticMode==='general'?'Geral':'Da turma'}</td><td>${r.proficiency}%</td><td>${esc(r.classification)}</td><td>${esc(r.validity||'Válido')}</td><td>${r.correct}/${r.answered}</td><td>${duration(r.durationSeconds)}</td><td>${r.integrity?.score??100}%</td><td>${new Date(r.endedAt).toLocaleString('pt-BR')}</td><td><div class="action-cluster"><button class="btn btn-primary btn-small" data-pdf-row="${esc(r.id)}">PDF</button><button class="btn btn-secondary btn-small" data-detail="${esc(r.id)}">Detalhes</button></div></td></tr>`).join('')}</tbody></table></div>`;host.querySelectorAll('[data-detail]').forEach(b=>b.onclick=()=>openDetail(list.find(r=>r.id===b.dataset.detail)));host.querySelectorAll('[data-pdf-row]').forEach(b=>b.onclick=async()=>{const r=list.find(x=>x.id===b.dataset.pdfRow);const txt=b.textContent;b.disabled=true;b.textContent='...';try{await downloadResultPDF(r,`resultado-${safeFileName(r.student?.name)}.pdf`);toast('PDF gerado.','good')}catch{toast('Falha ao gerar PDF.','bad')}finally{b.disabled=false;b.textContent=txt}})}
async function importFiles(files){let ok=0,bad=0;for(const file of files){try{if(file.size>8*1024*1024)throw new Error('Arquivo muito grande.');let data;if(file.name.endsWith('.agvresult'))data=await decryptObject(await file.arrayBuffer());else data=JSON.parse(await file.text());const arr=Array.isArray(data)?data:[data];if(arr.length>250)throw new Error('Quantidade de resultados acima do limite.');for(const r of arr){try{records.push(sanitizeDiagnosticResult(r));ok++}catch{bad++}}}catch{bad++}}records=dedupe(records);await saveTeacherRecords(records);renderTable();toast(`${ok} resultado(s) importado(s).${bad?` ${bad} item(ns) rejeitado(s).`:''}`,bad?'warn':'good')}
function openDetail(r){const modal=document.createElement('div');modal.className='modal';modal.innerHTML=`<div class="modal-card"><span class="hero-kicker">${r.diagnosticMode==='general'?'Diagnóstico geral':'Diagnóstico da turma'}</span><h2>${esc(r.student.name)}</h2><p>${CLASSES[r.selectedClass]?.label||r.selectedClass} · ${new Date(r.endedAt).toLocaleString('pt-BR')}</p><div class="metric-grid"><div class="metric"><small>Proficiência</small><strong>${r.proficiency}%</strong></div><div class="metric"><small>Acertos</small><strong>${r.correct}/${r.answered}</strong></div><div class="metric"><small>XP motivacional</small><strong>${r.xp}</strong></div><div class="metric"><small>Integridade</small><strong>${r.integrity?.score??100}%</strong></div><div class="metric"><small>Validade</small><strong>${esc(r.validity||'Válido')}</strong></div><div class="metric"><small>Avançadas</small><strong>${r.advancedAccuracy??0}%</strong></div></div><h3>Áreas</h3><div class="area-results">${r.areas.map(a=>`<div class="area-result"><span>${esc(a.area)}</span><div class="area-bar"><i style="width:${a.percent}%"></i></div><strong>${a.percent}%</strong></div>`).join('')}</div><h3>Registro</h3><div class="info-grid"><div class="info-card"><h4>Modalidade</h4><p>${esc(r.diagnosticModeLabel)}</p></div><div class="info-card"><h4>Perfil utilizado</h4><p>${r.classProfileUsed?CLASSES[r.classProfileUsed].short:'Banco geral'}</p></div><div class="info-card"><h4>Eventos de integridade</h4><p>${r.integrity?.events?.length||0}</p></div><div class="info-card"><h4>Tempo</h4><p>${duration(r.durationSeconds)}</p></div><div class="info-card"><h4>Termos</h4><p>${r.termsAcceptance?.recordIntegrity?'Aceite validado':'Não validado'}</p></div></div><div class="stage-actions"><button class="btn btn-primary" data-pdf>Baixar PDF</button><button class="btn btn-secondary" data-download>Baixar JSON</button><button class="btn btn-secondary" data-close>Fechar</button></div></div>`;document.body.appendChild(modal);modal.querySelector('[data-close]').onclick=()=>modal.remove();modal.querySelector('[data-download]').onclick=()=>downloadJSON(r,`resultado-${safeFileName(r.student.name)}.json`);modal.querySelector('[data-pdf]').onclick=async e=>{const b=e.currentTarget,txt=b.textContent;b.disabled=true;b.textContent='Gerando PDF...';try{await downloadResultPDF(r,`resultado-${safeFileName(r.student.name)}.pdf`);toast('PDF gerado.','good')}catch(err){console.error(err);toast('Não foi possível gerar o PDF.','bad')}finally{b.disabled=false;b.textContent=txt}}}
function csvValue(value){const text=cleanText(value,2000);return /^[=+\-@]/.test(text)?`'${text}`:text}
function exportCSV(){const rows=[['Nome','Turma','Modalidade','Proficiência','Classificação','Validade','Acertos','Respondidas','XP_motivacional','Tempo_segundos','Integridade','Data','ID'],...records.map(r=>[r.student?.name,r.selectedClass,r.diagnosticModeLabel,r.proficiency,r.classification,r.validity||'Válido',r.correct,r.answered,r.xp,r.durationSeconds,r.integrity?.score??100,r.endedAt,r.id])];const csv='\ufeff'+rows.map(row=>row.map(v=>`"${csvValue(v).replace(/"/g,'""')}"`).join(';')).join('\n');downloadBlob(new Blob([csv],{type:'text/csv;charset=utf-8'}),'resultados-agv.csv')}
function duration(s){s=Math.max(0,Math.floor(s||0));return `${String(Math.floor(s/60)).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`}
function safe(v){return String(v).normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-zA-Z0-9]+/g,'-').toLowerCase()}
function dedupe(list){const map=new Map();for(const item of list){try{const r=sanitizeDiagnosticResult(item);if(r.id)map.set(r.id,r)}catch{}}return [...map.values()].sort((a,b)=>new Date(b.endedAt)-new Date(a.endedAt))}

if('serviceWorker' in navigator){window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js').catch(error=>console.warn('Modo offline não pôde ser ativado.',error)))}
