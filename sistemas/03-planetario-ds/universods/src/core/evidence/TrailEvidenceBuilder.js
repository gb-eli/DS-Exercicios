const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const duration=ms=>{const total=Math.max(0,Math.floor(Number(ms||0)/1000)),h=Math.floor(total/3600),m=Math.floor(total%3600/60),s=total%60;return `${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`;};
const hash=text=>{let value=2166136261;for(const char of text){value^=char.charCodeAt(0);value=Math.imul(value,16777619);}return (value>>>0).toString(16).padStart(8,'0').toUpperCase();};
export class TrailEvidenceBuilder {
  build({plan,profile,status,appVersion='29.0.0'}){const report={schema:'cosmos-ds-trail-evidence-v1',generatedAt:new Date().toISOString(),appVersion,student:{id:profile.id,name:profile.name,className:profile.className,callsign:profile.callsign,level:profile.level,xp:profile.xp},trail:{id:plan.id,title:plan.title,role:plan.role,mission:plan.mission,className:plan.className,accessCode:plan.accessCode,minimumTotalMinutes:plan.minimumTotalMinutes,classroomUrl:plan.classroomUrl,classroomInstructions:plan.classroomInstructions},session:{state:status.state,startedAt:status.startedAt,completedAt:status.completedAt,activeMs:status.activeMs,activeLabel:duration(status.activeMs),progress:{completed:status.completed,total:status.total,percent:status.percent}},steps:plan.steps.map(step=>{const record=(status.records||[]).find(item=>item.id===step.id);return {...step,status:record?'completed':'pending',record:record||null,evidence:status.evidenceByStep?.[step.id]||record?.evidence||''};}),events:(status.events||[]).slice(-150)};report.validationCode=hash(JSON.stringify(report));return report;}
  toJson(report){return JSON.stringify(report,null,2);}
  toHtml(report){return `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Evidência — ${esc(report.trail.title)}</title><style>@page{size:A4;margin:14mm}*{box-sizing:border-box}body{font-family:Arial,sans-serif;margin:0;color:#132039;background:#edf3f8}main{max-width:930px;margin:22px auto;background:#fff;padding:32px;border-radius:18px;box-shadow:0 15px 55px #17314a22}h1{margin:4px 0 8px;font-size:28px}h2{margin-top:28px;font-size:18px}.meta{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.meta div,.step{border:1px solid #d7e3ec;border-radius:11px;padding:12px}.meta span,.step small{display:block;color:#617386;font-size:11px}.meta b{display:block;margin-top:4px}.step{margin:9px 0}.step.done{border-left:5px solid #16a578}.step.pending{border-left:5px solid #d89b2d}.evidence{padding:9px;background:#f4f8fb;border-radius:8px;white-space:pre-wrap}.classroom{margin-top:24px;padding:15px;border:1px solid #a8cfee;background:#eef8ff;border-radius:12px}.code{font-family:monospace}.print{margin:18px 0;border:0;border-radius:9px;padding:10px 14px;background:#126b9b;color:white;font-weight:bold}@media print{body{background:#fff}main{box-shadow:none;margin:0;padding:0}.print{display:none}}</style></head><body><main><button class="print" onclick="print()">Imprimir / salvar em PDF</button><small>COSMOS DS · Evidência de jornada</small><h1>${esc(report.trail.title)}</h1><p>${esc(report.trail.mission)}</p><section class="meta"><div><span>Estudante</span><b>${esc(report.student.name)}</b><small>${esc(report.student.className)} · ${esc(report.student.callsign)}</small></div><div><span>Progresso</span><b>${report.session.progress.completed}/${report.session.progress.total} · ${report.session.progress.percent}%</b><small>${esc(report.session.state)}</small></div><div><span>Tempo ativo</span><b>${esc(report.session.activeLabel)}</b><small>Mínimo planejado: ${report.trail.minimumTotalMinutes} min</small></div></section><h2>Etapas e evidências</h2>${report.steps.map((step,index)=>`<article class="step ${step.status==='completed'?'done':'pending'}"><small>ETAPA ${index+1} · ${esc(step.moduleId)} · ${step.minimumMinutes} min</small><h3>${esc(step.title)}</h3><p>${esc(step.objective)}</p><div class="evidence"><b>Evidência:</b> ${esc(step.evidence||'Não registrada.')}</div></article>`).join('')}<section class="classroom"><b>Entrega</b><p>${esc(report.trail.classroomInstructions)}</p>${report.trail.classroomUrl?`<p>Atividade configurada: <span class="code">${esc(report.trail.classroomUrl)}</span></p>`:''}</section><p class="code">Código de validação: ${esc(report.validationCode)} · Gerado em ${esc(report.generatedAt)}</p></main></body></html>`;}
}
