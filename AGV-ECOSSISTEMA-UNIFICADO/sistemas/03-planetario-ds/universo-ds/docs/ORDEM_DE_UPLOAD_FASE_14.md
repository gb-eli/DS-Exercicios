# Ordem de atualização no GitHub — Fase 14

## Opção recomendada

Extraia `cosmos-ds-fase-14-arquivos-alterados.zip` sobre a raiz atual do projeto e confirme a substituição dos arquivos existentes.

## Ordem manual

1. Envie as novas pastas `src/core/lunar-mars-remaster/` e `src/modules/lunar-mars-remaster/`.
2. Envie `src/data/lunarMarsRemasterSystems.js`.
3. Envie `src/rendering/PlanetaryExplorationRenderer.js`.
4. Envie `src/workers/planetary-exploration.worker.js`.
5. Substitua `src/core/modules/ModuleRegistry.js` e `src/styles.css`.
6. Substitua scripts, testes, `package.json`, `index.html` e `service-worker.js`.
7. Envie a documentação da Fase 14.
8. Faça recarga forçada ou limpe o cache do Service Worker anterior.

Nenhuma pasta antiga deve ser apagada.
