import { supabase } from './supabase.js';
import { SCHOOL_EMAIL_DOMAIN, ACTIVITY_URL, LOBBY_VERSION } from './config.js';
const $=id=>document.getElementById(id),WORLD={w:1600,h:1000},R=21;
const state={user:null,profile:null,currentClass:null,classes:[],exercises:[],studentReleases:[],classReleases:[],progress:[],others:[],available:[],scheduled:[],keys:new Set(),player:{x:800,y:500,area:'central'},lastFrame:0,lastPresence:0,nearPortal:null,nearStudent:null,toastTimer:null,stopped:false,lastTargetSignal:''};
const zones=[
 {key:'1ds',code:'1DS-A-MANHA',label:'1DS',name:'1ª Série DS',x:90,y:90,w:560,h:330,accent:'#36d2ff',portal:{x:370,y:245,kind:'door',label:'Porta Neon'}},
 {key:'2ds',code:'2DS-A-MANHA',label:'2DS',name:'2ª Série DS',x:950,y:90,w:560,h:330,accent:'#51e7a3',portal:{x:1230,y:245,kind:'pipe',label:'Cano Tech'}},
 {key:'3ds',code:'3DS-C-MANHA',label:'3DS',name:'3ª Série DS',x:90,y:580,w:560,h:330,accent:'#b58cff',portal:{x:370,y:745,kind:'portal',label:'Portal Quantum'}},
 {key:'sub',code:'DS-SUB-NOITE',label:'SUB',name:'DS Subsequente',x:950,y:580,w:560,h:330,accent:'#ffae63',portal:{x:1230,y:745,kind:'gate',label:'Arcade Gate'}}];
const isStaff=()=>['teacher','admin','super_admin'].includes(state.profile?.role),roleLabel=r=>r==='teacher'?'Professor':r==='super_admin'?'Super Admin':r==='admin'?'Administrador':'Aluno';
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function showLogin(on){$('login').classList.toggle('hidden',!on);$('game-shell').classList.toggle('hidden',on)}function msg(t='',e=false){$('login-message').textContent=t;$('login-message').classList.toggle('error',e)}function email(v){return String(v||'').trim().toLowerCase()}function zoneAt(x,y){return zones.find(z=>x>=z.x&&x<=z.x+z.w&&y>=z.y&&y<=z.y+z.h)||null}function ownZone(){return zones.find(z=>z.code===state.currentClass?.code)||null}
async function signIn(mail,password){const {error}=await supabase.auth.signInWithPassword({email:mail,password});if(!error)return;const cgm=/^\d{6,12}$/.test(password);if(!cgm)throw error;const {data,error:up}=await supabase.auth.signUp({email:mail,password,options:{data:{cgm:password},emailRedirectTo:location.href}});if(up)throw up;if(data?.session)return;const {error:again}=await supabase.auth.signInWithPassword({email:mail,password});if(again)throw again}
async function activeBlock(){if(state.profile?.role!=='student'||!state.user)return null;const {data}=await supabase.from('lobby_blocks').select('blocked_until,reason').eq('student_id',state.user.id).gt('blocked_until',new Date().toISOString()).maybeSingle();return data||null}
function showKicked(block){state.stopped=true;$('game-shell').classList.add('hidden');$('login').classList.add('hidden');$('kicked').classList.remove('hidden');$('kicked-reason').textContent=block?.reason||'Você foi removido temporariamente do Lobby pela equipe.';$('kicked-until').textContent=block?.blocked_until?`Retorno permitido após ${new Date(block.blocked_until).toLocaleString('pt-BR')}.`:'Aguarde a liberação da equipe.'}
async function loadIdentity(){const {data:{user}}=await supabase.auth.getUser();if(!user)return false;const {data:p,error}=await supabase.from('profiles').select('id,full_name,email,role,active,must_change_password').eq('id',user.id).single();if(error)throw error;if(!p.active)throw new Error('Seu acesso está inativo.');if(p.must_change_password){location.href='../atividades/';return false}if(!['student','teacher','admin','super_admin'].includes(p.role))throw new Error('Perfil sem acesso ao Lobby.');state.user=user;state.profile=p;const {data:all}=await supabase.from('classes').select('id,code,name,shift,school_year').in('code',zones.map(z=>z.code)).eq('active',true);state.classes=all||[];
 if(p.role==='student'){const {data:m,error:me}=await supabase.from('class_memberships').select('class_id').eq('user_id',user.id).eq('active',true).order('is_primary',{ascending:false}).limit(1);if(me)throw me;if(!m?.length)throw new Error('Sua turma ainda não foi vinculada.');const {data:cls,error:ce}=await supabase.from('classes').select('id,code,name,shift,school_year').eq('id',m[0].class_id).single();if(ce)throw ce;state.currentClass=cls;const block=await activeBlock();if(block){showKicked(block);return false}}
 $('player-name').textContent=p.full_name||roleLabel(p.role);$('player-class').textContent=p.role==='student'?(state.currentClass?.name||'Aluno'):`${roleLabel(p.role)} • Lobby`;$('staff-mode').classList.toggle('hidden',!isStaff());$('staff-moderation').classList.toggle('hidden',!isStaff());return true}
function releaseState(ex){const now=Date.now(),pr=state.progress.find(x=>x.exercise_id===ex.id);if(pr?.security_locked||pr?.status==='blocked')return{available:false,reason:'Bloqueada por segurança'};const r=state.studentReleases.find(x=>x.exercise_id===ex.id)||state.classReleases.find(x=>x.exercise_id===ex.id);if(!r)return{available:!ex.default_locked,reason:ex.default_locked?'Aguardando liberação do professor':'Disponível'};if(!r.enabled)return{available:false,reason:'Bloqueada pelo professor'};if(r.release_at&&Date.parse(r.release_at)>now)return{available:false,reason:'Programada',releaseAt:r.release_at};if(r.lock_at&&Date.parse(r.lock_at)<=now)return{available:false,reason:'Encerrada'};return{available:true,reason:'Liberada'}}
async function loadActivities(){if(state.profile?.role!=='student'||!state.currentClass)return;const cid=state.currentClass.id,{data:cs}=await supabase.from('class_subjects').select('subject_id').eq('class_id',cid).eq('active',true),ids=(cs||[]).map(x=>x.subject_id);if(!ids.length){state.exercises=[];state.available=[];return}const [{data:ex},{data:sr},{data:cr},{data:pr}]=await Promise.all([supabase.from('exercises').select('id,class_id,subject_id,exercise_number,title,description,version,default_locked').in('subject_id',ids).eq('active',true).eq('visible',true).order('exercise_number'),supabase.from('exercise_releases').select('exercise_id,enabled,release_at,lock_at,updated_at').eq('student_id',state.user.id).order('updated_at',{ascending:false}),supabase.from('exercise_releases').select('exercise_id,enabled,release_at,lock_at,updated_at').is('student_id',null).eq('class_id',cid).order('updated_at',{ascending:false}),supabase.from('student_exercises').select('exercise_id,status,progress_percent,security_locked,security_lock_reason,completed_at').eq('student_id',state.user.id)]);const uniq=rows=>{const s=new Set();return(rows||[]).filter(r=>!s.has(r.exercise_id)&&s.add(r.exercise_id))};state.exercises=(ex||[]).filter(e=>!e.class_id||e.class_id===cid);state.studentReleases=uniq(sr);state.classReleases=uniq(cr);state.progress=pr||[];state.available=state.exercises.filter(e=>releaseState(e).available);state.scheduled=state.exercises.map(ex=>({ex,s:releaseState(ex)})).filter(x=>!x.s.available&&x.s.releaseAt).sort((a,b)=>Date.parse(a.s.releaseAt)-Date.parse(b.s.releaseAt))}
async function boot(){if(!await loadIdentity())return;await loadActivities();showLogin(false);await presence(true);poll();requestAnimationFrame(loop)}
async function presence(force=false,target=null,emote=null){if(!state.user||state.stopped)return;const n=performance.now();if(!force&&n-state.lastPresence<5000)return;state.lastPresence=n;const payload={student_id:state.user.id,x:Math.round(state.player.x),y:Math.round(state.player.y),area:state.player.area};if(emote){payload.emote=emote;payload.emote_until=new Date(Date.now()+4500).toISOString();payload.interaction_target_id=target||null}try{const {error}=await supabase.from('lobby_presence').upsert(payload,{onConflict:'student_id'});if(error&&state.profile?.role==='student'){const b=await activeBlock();if(b)showKicked(b)}}catch(_){}}
async function emote(kind,target=null){await presence(true,target,kind)}
async function poll(){if(!state.user||state.stopped)return;try{const cutoff=new Date(Date.now()-20000).toISOString(),{data}=await supabase.from('lobby_presence').select('student_id,class_id,display_name,participant_role,x,y,area,emote,emote_until,interaction_target_id,updated_at').gt('updated_at',cutoff);state.others=(data||[]).filter(x=>x.student_id!==state.user.id);$('online-count').textContent=`${state.others.length+1} online`;const sig=state.others.find(x=>x.interaction_target_id===state.user.id&&x.emote&&Date.parse(x.emote_until||0)>Date.now());if(sig){const key=`${sig.student_id}:${sig.emote_until}`;if(key!==state.lastTargetSignal){state.lastTargetSignal=key;toast(`${sig.display_name} enviou ${sig.emote==='wave'?'👋':sig.emote==='like'?'👍':'✨'} para você.`)}}if(state.profile?.role==='student'){const b=await activeBlock();if(b){showKicked(b);return}}}catch(_){}finally{if(!state.stopped)setTimeout(poll,4000)}}
function toast(t){const e=$('toast');e.textContent=t;e.classList.remove('hidden');clearTimeout(state.toastTimer);state.toastTimer=setTimeout(()=>e.classList.add('hidden'),2800)}
function portalState(z){if(isStaff())return{open:false,text:`${z.label}: circulação/observação liberada. Liberações pedagógicas continuam no painel.`};if(z.code!==state.currentClass?.code)return{open:false,text:`Área de convivência ${z.label}. As atividades são exclusivas da turma.`};if(state.available.length)return{open:true,text:`${state.available.length} atividade${state.available.length===1?'':'s'} liberada${state.available.length===1?'':'s'}.`};const n=state.scheduled[0];if(n)return{open:false,text:`Próxima abertura: ${new Date(n.s.releaseAt).toLocaleString('pt-BR')}.`};return{open:false,text:'Portal fechado. Aguardando o professor liberar uma atividade.'}}
function showActivities(){if(!state.available.length){toast('O portal ainda está fechado pelo professor.');return}$('activity-title').textContent=`Atividades liberadas — ${state.currentClass.name}`;$('activity-subtitle').textContent='Escolha uma atividade para atravessar o portal.';$('activity-list').innerHTML=state.available.map(ex=>{const p=state.progress.find(x=>x.exercise_id===ex.id),st=p?.status==='completed'?'Concluída':p?.status==='in_progress'?`${Math.round(Number(p.progress_percent||0))}% em andamento`:'Disponível';return `<article class="activity"><span class="num">${String(ex.exercise_number).padStart(2,'0')}</span><div><strong>${esc(ex.title)}</strong><small>${esc(st)}</small></div><button class="primary" data-exercise="${esc(ex.id)}" type="button">Entrar</button></article>`}).join('');$('activity-list').querySelectorAll('[data-exercise]').forEach(b=>b.onclick=()=>{const ex=state.exercises.find(x=>x.id===b.dataset.exercise);if(!ex||!releaseState(ex).available)return toast('A atividade não está liberada.');location.href=`${ACTIVITY_URL}?exercise=${encodeURIComponent(ex.id)}&from=lobby`});$('activity-modal').classList.remove('hidden')}
function openStaffTarget(o){if(!isStaff()||o?.participant_role!=='student')return;$('student-name').textContent=o.display_name||'Aluno';$('student-context').textContent=`Aluno online • ${className(o.class_id)}`;$('staff-student-modal').dataset.student=o.student_id;$('kick-reason').value='';$('kick-duration').value='15';$('staff-student-modal').classList.remove('hidden')}
async function kickStudent(){const id=$('staff-student-modal').dataset.student,btn=$('kick-student');if(!id)return;const name=$('student-name').textContent||'este aluno',mins=Number($('kick-duration').value)||15,reason=$('kick-reason').value.trim();if(!confirm(`Expulsar ${name} do Lobby por ${mins} minuto${mins===1?'':'s'}?\n\nA conta, a matrícula e as atividades não serão afetadas.`))return;btn.disabled=true;try{const {data,error}=await supabase.functions.invoke('lobby-moderation',{body:{action:'kick',student_id:id,duration_minutes:mins,reason}});if(error||data?.error)throw new Error(data?.error||error?.message);$('staff-student-modal').classList.add('hidden');toast(`Aluno removido até ${new Date(data.blocked_until).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}.`)}catch(e){toast(e.message==='student_out_of_scope'?'Você só pode expulsar alunos das suas turmas.':'Não foi possível remover o aluno.')}finally{btn.disabled=false}}
async function loadModeration(){if(!isStaff())return;const list=$('moderation-list');list.replaceChildren(Object.assign(document.createElement('p'),{className:'muted',textContent:'Carregando…'}));$('staff-moderation-modal').classList.remove('hidden');try{const {data,error}=await supabase.functions.invoke('lobby-moderation',{body:{action:'list_blocks'}});if(error||data?.error)throw new Error(data?.error||error?.message);const rows=data?.blocks||[];list.replaceChildren();if(!rows.length){const empty=document.createElement('div');empty.className='moderation-empty';empty.textContent='Nenhuma expulsão ativa no seu escopo.';list.appendChild(empty);return}for(const row of rows){const card=document.createElement('div');card.className='moderation-row';const info=document.createElement('div'),name=document.createElement('strong'),meta=document.createElement('small'),reason=document.createElement('small'),btn=document.createElement('button');name.textContent=row.student_name||'Aluno';meta.textContent=`${row.class_name||'Turma'} • retorno após ${new Date(row.blocked_until).toLocaleString('pt-BR')}`;reason.textContent=`Motivo: ${row.reason||'Não informado'}`;btn.type='button';btn.textContent='Readmitir agora';btn.onclick=()=>unblockStudent(row.student_id,row.student_name||'o aluno');info.append(name,meta,reason);card.append(info,btn);list.appendChild(card)}}catch(e){list.replaceChildren(Object.assign(document.createElement('div'),{className:'moderation-empty',textContent:'Não foi possível carregar as expulsões ativas.'}))}}
async function unblockStudent(id,name){if(!confirm(`Readmitir ${name} no Lobby agora?`))return;try{const {data,error}=await supabase.functions.invoke('lobby-moderation',{body:{action:'unblock',student_id:id}});if(error||data?.error)throw new Error(data?.error||error?.message);toast('Aluno readmitido no Lobby.');await loadModeration()}catch(e){toast(e.message==='student_out_of_scope'?'Aluno fora do seu escopo.':'Não foi possível readmitir o aluno.')}}
function interact(){if(state.nearStudent&&isStaff())return openStaffTarget(state.nearStudent);if(state.nearPortal){const z=state.nearPortal.zone,ps=portalState(z);if(isStaff())return toast(ps.text);if(z.code!==state.currentClass?.code)return toast(`Você pode visitar ${z.label}, mas as atividades pertencem àquela turma.`);ps.open?showActivities():toast(ps.text)}}
function update(dt){let dx=0,dy=0;if(state.keys.has('ArrowLeft')||state.keys.has('KeyA'))dx--;if(state.keys.has('ArrowRight')||state.keys.has('KeyD'))dx++;if(state.keys.has('ArrowUp')||state.keys.has('KeyW'))dy--;if(state.keys.has('ArrowDown')||state.keys.has('KeyS'))dy--;if(dx&&dy){dx*=.7071;dy*=.7071}state.player.x=Math.max(R,Math.min(WORLD.w-R,state.player.x+dx*260*dt));state.player.y=Math.max(R,Math.min(WORLD.h-R,state.player.y+dy*260*dt));const z=zoneAt(state.player.x,state.player.y);state.player.area=z?.key||'central';$('area-label').textContent=z?`Área ${z.label} — ${z.name}`:'Praça Central';state.nearPortal=null;for(const zone of zones)if(Math.hypot(state.player.x-zone.portal.x,state.player.y-zone.portal.y)<92){state.nearPortal={zone};break}state.nearStudent=null;if(isStaff()){let best=90;for(const o of state.others)if(o.participant_role==='student'){const d=Math.hypot(state.player.x-o.x,state.player.y-o.y);if(d<best){best=d;state.nearStudent=o}}}renderInteraction();presence()}
function renderInteraction(){const b=$('interaction');if(state.nearStudent&&isStaff()){b.classList.remove('hidden');$('interaction-title').textContent=`Interagir — ${state.nearStudent.display_name}`;$('interaction-text').textContent=`${className(state.nearStudent.class_id)} • reações e moderação`;$('interaction-button').textContent='Opções';return}if(!state.nearPortal){b.classList.add('hidden');return}const z=state.nearPortal.zone,ps=portalState(z);b.classList.remove('hidden');$('interaction-title').textContent=`${z.portal.label} — ${z.label}`;$('interaction-text').textContent=ps.text;$('interaction-button').textContent=ps.open?'Abrir portal':'Ver estado'}
function rr(c,x,y,w,h,r){c.beginPath();c.roundRect(x,y,w,h,r)}function className(id){return state.classes.find(c=>c.id===id)?.name||'Outra turma'}function accent(id){const c=state.classes.find(x=>x.id===id);return zones.find(z=>z.code===c?.code)?.accent||'#8aa0af'}
function drawAvatar(c,o,self=false){const staff=['teacher','admin','super_admin'].includes(o.participant_role),a=staff?'#ffd166':self?accent(state.currentClass?.id):accent(o.class_id);c.save();c.translate(Number(o.x)||0,Number(o.y)||0);c.shadowColor=a;c.shadowBlur=self||staff?15:7;c.fillStyle=a;c.beginPath();c.arc(0,0,20,0,Math.PI*2);c.fill();c.shadowBlur=0;c.fillStyle='#e9f7ff';c.beginPath();c.arc(0,-8,8,0,Math.PI*2);c.fill();c.fillStyle='#061019dd';rr(c,-50,28,100,23,10);c.fill();c.fillStyle=staff?'#ffe8a3':self?'#fff':'#bfd1dc';c.font='700 12px system-ui';c.textAlign='center';c.fillText(String(o.display_name||'Aluno').slice(0,16),0,44);if(staff){c.fillStyle='#3a2c00';rr(c,-23,-34,46,16,7);c.fill();c.fillStyle='#ffd166';c.font='900 9px system-ui';c.fillText(o.participant_role==='teacher'?'PROF':'ADM',0,-22)}if(o.emote&&Date.parse(o.emote_until||0)>Date.now()){c.font='24px system-ui';c.fillText({wave:'👋',like:'👍',spark:'✨'}[o.emote]||'',0,-43)}c.restore()}
function drawPortal(c,z,t){const ps=portalState(z),p=z.portal,pulse=(Math.sin(t/300)+1)/2;c.save();c.translate(p.x,p.y);c.strokeStyle=ps.open?z.accent:'#41515d';c.fillStyle=ps.open?'#0b2630':'#111820';c.lineWidth=6;if(ps.open){c.shadowColor=z.accent;c.shadowBlur=22+18*pulse}if(p.kind==='pipe'){c.fillRect(-52,-58,104,116);c.strokeRect(-52,-58,104,116);c.fillStyle=ps.open?z.accent:'#4a5963';c.fillRect(-70,-73,140,25)}else if(p.kind==='portal'){c.beginPath();c.ellipse(0,0,62,88,0,0,Math.PI*2);c.fill();c.stroke()}else if(p.kind==='gate'){c.fillRect(-66,-72,132,144);c.strokeRect(-66,-72,132,144)}else{c.beginPath();c.moveTo(-64,72);c.lineTo(-64,-30);c.quadraticCurveTo(-64,-84,0,-84);c.quadraticCurveTo(64,-84,64,-30);c.lineTo(64,72);c.closePath();c.fill();c.stroke()}c.shadowBlur=0;c.fillStyle='#eaf7ff';c.textAlign='center';c.font='900 14px system-ui';c.fillText(p.label,0,112);c.fillStyle=ps.open?z.accent:'#7d909d';c.font='700 12px system-ui';c.fillText(isStaff()?'OBSERVAÇÃO':ps.open?'ABERTO':'FECHADO',0,132);c.restore()}
function draw(t){const canvas=$('game'),c=canvas.getContext('2d');c.clearRect(0,0,WORLD.w,WORLD.h);c.fillStyle='#061018';c.fillRect(0,0,WORLD.w,WORLD.h);c.strokeStyle='#102431';for(let x=0;x<WORLD.w;x+=50){c.beginPath();c.moveTo(x,0);c.lineTo(x,WORLD.h);c.stroke()}for(let y=0;y<WORLD.h;y+=50){c.beginPath();c.moveTo(0,y);c.lineTo(WORLD.w,y);c.stroke()}const g=c.createRadialGradient(800,500,20,800,500,410);g.addColorStop(0,'#12384a');g.addColorStop(1,'#07131c');c.fillStyle=g;c.beginPath();c.arc(800,500,330,0,Math.PI*2);c.fill();c.textAlign='center';c.fillStyle='#8edfff';c.font='900 28px system-ui';c.fillText('AGV • PRAÇA CENTRAL',800,485);c.fillStyle='#7293a8';c.font='600 15px system-ui';c.fillText(isStaff()?'MODO EQUIPE • CIRCULAÇÃO, INTERAÇÃO E MODERAÇÃO':'CIRCULAÇÃO LIVRE • PORTAIS CONTROLADOS PELO PROFESSOR',800,514);for(const z of zones){const own=z.code===state.currentClass?.code;c.fillStyle='#09141c';rr(c,z.x,z.y,z.w,z.h,28);c.fill();c.strokeStyle=own?z.accent:'#263746';c.lineWidth=own?5:2;c.stroke();c.fillStyle=z.accent;c.textAlign='left';c.font='900 25px system-ui';c.fillText(z.label,z.x+24,z.y+39);c.fillStyle='#a7bdcb';c.font='700 15px system-ui';c.fillText(z.name,z.x+24,z.y+64);drawPortal(c,z,t)}for(const o of state.others)drawAvatar(c,o);drawAvatar(c,{x:state.player.x,y:state.player.y,display_name:(state.profile?.role==='teacher'?'Prof. ':state.profile?.role==='student'?'': 'ADM ')+(state.profile?.full_name||'Usuário').split(' ')[0],participant_role:state.profile?.role,class_id:state.currentClass?.id},true)}
function loop(ts){if(state.stopped)return;const dt=Math.min(.05,(ts-(state.lastFrame||ts))/1000);state.lastFrame=ts;update(dt);draw(ts);requestAnimationFrame(loop)}
window.addEventListener('keydown',e=>{if(['INPUT','TEXTAREA','SELECT'].includes(document.activeElement?.tagName))return;if(['ArrowUp','ArrowDown','ArrowLeft','ArrowRight','KeyW','KeyA','KeyS','KeyD'].includes(e.code)){e.preventDefault();state.keys.add(e.code)}if((e.code==='KeyE'||e.code==='Enter')&&!e.repeat){e.preventDefault();interact()}if(e.code==='Digit1')emote('wave',state.nearStudent?.student_id);if(e.code==='Digit2')emote('like',state.nearStudent?.student_id);if(e.code==='Digit3')emote('spark',state.nearStudent?.student_id)});window.addEventListener('keyup',e=>state.keys.delete(e.code));$('interaction-button').onclick=interact;$('touch-action').onclick=interact;$('activity-close').onclick=()=>$('activity-modal').classList.add('hidden');$('staff-student-close').onclick=()=>$('staff-student-modal').classList.add('hidden');$('staff-moderation-close').onclick=()=>$('staff-moderation-modal').classList.add('hidden');$('staff-moderation').onclick=loadModeration;$('kick-student').onclick=kickStudent;document.querySelectorAll('[data-target-emote]').forEach(b=>b.onclick=()=>emote(b.dataset.targetEmote,$('staff-student-modal').dataset.student).then(()=>toast('Interação enviada.')));document.querySelectorAll('[data-emote]').forEach(b=>b.onclick=()=>emote(b.dataset.emote));document.querySelectorAll('[data-key]').forEach(b=>{const code=b.dataset.key,on=e=>{e.preventDefault();state.keys.add(code)},off=e=>{e.preventDefault();state.keys.delete(code)};b.addEventListener('pointerdown',on);b.addEventListener('pointerup',off);b.addEventListener('pointercancel',off)});$('logout').onclick=async()=>{try{await supabase.from('lobby_presence').delete().eq('student_id',state.user.id)}catch(_){}await supabase.auth.signOut();location.reload()};$('kicked-check').onclick=()=>location.reload();document.addEventListener('visibilitychange',()=>{if(!document.hidden)loadActivities().catch(()=>{})});setInterval(()=>loadActivities().catch(()=>{}),15000);
$('login-form').addEventListener('submit',async e=>{e.preventDefault();const btn=e.submitter,mail=email($('email').value),password=$('password').value;if(!mail.endsWith(SCHOOL_EMAIL_DOMAIN))return msg(`Use o e-mail institucional ${SCHOOL_EMAIL_DOMAIN}.`,true);btn.disabled=true;msg('Entrando no lobby…');try{await signIn(mail,password);await boot();msg()}catch(err){console.error(err);msg('E-mail ou senha inválidos. Aluno no primeiro acesso: use o CGM. Equipe: use a senha individual fornecida pelo administrador.',true);try{await supabase.auth.signOut()}catch(_){}}finally{btn.disabled=false}});(async()=>{try{const {data:{session}}=await supabase.auth.getSession();if(session){await boot();return}}catch(e){console.warn(e)}showLogin(true)})();console.info(`AGV Lobby DS ${LOBBY_VERSION}`);
