# Prompt de Integração — v0.9.4.4

Preserve `DS_HUMANOID_VOXEL_RIG_2`, os 28 clips e os 16 slots oficiais. Para reproduzir clips use `DSStore.playEmote(clip)` ou `DSAvatarViewer.play(clip)`. Para demonstração isolada use `DSAvatarViewer.setRigFocus('full'|'core'|'arms'|'legs')`. Não renomeie bones nem slots em integrações externas.
