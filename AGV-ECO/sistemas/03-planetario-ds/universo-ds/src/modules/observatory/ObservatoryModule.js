import { OBSERVATORY_TIMELINE, TELESCOPES, TARGETS, FILTERS, SPECTRUM_CHALLENGES, OBSERVATION_CHALLENGES } from '../../data/observatorySystems.js';
import { TelescopeSystem } from '../../core/observatory/TelescopeSystem.js';
import { ImagePipeline } from '../../core/observatory/ImagePipeline.js';
import { SpectrumAnalyzer } from '../../core/observatory/SpectrumAnalyzer.js';
import { ObservationDatabase } from '../../core/observatory/ObservationDatabase.js';
import { UniverseSceneRenderer } from '../../rendering/UniverseSceneRenderer.js';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmt=(v,d=1)=>Number(v??0).toLocaleString('pt-BR',{minimumFractionDigits:d,maximumFractionDigits:d});
class ObservatoryModule{
 constructor(){this.container=null;this.context=null;this.tab='planetarium';this.timelineId='hubble';this.system=new TelescopeSystem();this.pipeline=new ImagePipeline();this.spectrum=new SpectrumAnalyzer();this.db=new ObservationDatabase();this.renderer=null;this.worker=null;this.frame=null;this.filters=new Set(['R','G','B']);this.exposure=80;this.noise=.16;this.calibration=true;this.contrast=1.18;this.gamma=.86;this.diagnostics=null;this.challengeFeedback='';this.spectrumFeedback='';this.certified=false;this.onClick=e=>this.click(e);this.onChange=e=>this.change(e);}
 mount(container,context){this.container=container;this.context=context;this.certified=context.profileStore.hasCompleted('observatory-certification');const p=context.profileStore.active();const saved=context.profileStore.storage.get(`observatory-db-${p.id}`,null);this.db=new ObservationDatabase(saved?.records??[]);this.container.addEventListener('click',this.onClick);this.container.addEventListener('change',this.onChange);this.configureWorker();this.render();}
 unmount(){this.worker?.terminate();this.worker=null;this.renderer?.destroy();this.renderer=null;this.container?.removeEventListener('click',this.onClick);this.container?.removeEventListener('change',this.onChange);this.container=null;}
 configureWorker(){try{this.worker=new Worker(new URL('../../workers/observatory.worker.js',import.meta.url),{type:'module'});this.worker.onmessage=e=>{if(e.data.type==='frame'){this.frame=e.data.payload;this.drawFrame();this.updateStats();}};this.worker.onerror=()=>{this.worker?.terminate();this.worker=null;this.context.toast('Pipeline em Worker indisponível; usando processamento local.');};this.worker.postMessage({type:'quality',payload:{quality:this.context.settingsStore.getProfile().id}});}catch{this.worker=null;}}
 process(){const target=this.system.target();const payload={scene:target.scene,exposure:this.exposure,noise:this.noise,calibration:this.calibration,channels:[...this.filters],seed:target.id.length*31};if(this.worker)this.worker.postMessage({type:'process',payload});else{this.frame={...this.pipeline.generate(payload),processingMs:0,quality:'fallback'};this.drawFrame();this.updateStats();}}
 save(){const profile=this.context.profileStore.active();this.context.profileStore.storage.set(`observatory-db-${profile.id}`,{records:this.db.list()});}
 render(){this.renderer?.destroy();this.renderer=null;this.container.innerHTML=`<section class="module-stage observatory-module"><header class="module-hero observatory-hero"><div><span class="eyebrow">Fase 9 · Observatório e Universo Profundo</span><h2>Transforme fótons em <span>dados científicos rastreáveis.</span></h2><p>Navegue em 360°, escolha o instrumento correto, calibre imagens, componha canais, leia espectros e preserve a proveniência das observações.</p></div><div class="module-hero-metrics"><article><b>13</b><span>módulos ativos</span></article><article><b>360°</b><span>universo navegável</span></article><article><b>GLSL</b><span>nebulosa volumétrica</span></article></div></header><nav class="lab-tabs observatory-tabs">${this.tb('planetarium','Planetário 3D')}${this.tb('telescopes','Telescópios')}${this.tb('imaging','Imagem científica')}${this.tb('spectrum','Espectroscopia')}${this.tb('database','Observações')}</nav><div class="observatory-tab-host">${this.tabMarkup()}</div><section class="section glass certification-panel">${this.certMarkup()}</section></section>`;if(this.tab==='planetarium')this.afterScene();if(this.tab==='imaging'){this.process();}}
 tb(id,label){return `<button class="lab-tab ${this.tab===id?'active':''}" data-action="obs-tab" data-tab="${id}">${label}</button>`;}
 tabMarkup(){if(this.tab==='planetarium')return this.planetarium();if(this.tab==='telescopes')return this.telescopes();if(this.tab==='imaging')return this.imaging();if(this.tab==='spectrum')return this.spectroscopy();return this.database();}
 planetarium(){const selected=OBSERVATORY_TIMELINE.find(i=>i.id===this.timelineId)??OBSERVATORY_TIMELINE[0],target=this.system.target();return `<div class="observatory-scene-grid"><section class="section glass observatory-visual"><div class="simulation-toolbar"><div><span class="eyebrow">Planetário procedural</span><h3>${esc(target.label)}</h3></div><div class="toolbar-actions"><button class="button secondary small" data-action="obs-mode" data-mode="nebula">Nebulosa</button><button class="button secondary small" data-action="obs-mode" data-mode="galaxy">Galáxia</button><button class="button secondary small" data-action="obs-mode" data-mode="supernova">Supernova</button><button class="button secondary small" data-action="obs-mode" data-mode="blackhole">Buraco negro</button><button class="button secondary small" data-action="obs-fullscreen">Tela cheia</button></div></div><div class="universe-scene-wrap"><canvas id="universe-scene" aria-label="Universo profundo procedural navegável em 360 graus"></canvas><div class="universe-hud"><span>ALVO <b>${esc(target.label)}</b></span><span>DIST <b>${esc(target.distance)}</b></span><span>FOV <b id="obs-zoom">1,00×</b></span></div></div><p class="canvas-help">Arraste para olhar em 360°, use a roda para alterar o campo de visão e explore diferentes estruturas.</p><div class="graphics-audit" id="universe-diagnostics"><span>Renderizador aguardando inicialização.</span></div></section><aside class="side-stack"><section class="panel glass"><h3>Alvo científico</h3><label class="field"><span>Objeto</span><select data-obs="target">${TARGETS.map(i=>`<option value="${i.id}" ${i.id===target.id?'selected':''}>${esc(i.label)}</option>`).join('')}</select></label><div class="target-features">${target.features.map(i=>`<span>${esc(i)}</span>`).join('')}</div><button class="button primary" data-action="obs-complete-planetarium">Registrar exploração · 240 XP</button></section><section class="panel glass"><h3>Evolução dos observatórios</h3><div class="compact-timeline">${OBSERVATORY_TIMELINE.map(i=>`<button class="compact-era ${i.id===selected.id?'active':''}" data-action="obs-era" data-era="${i.id}"><b>${i.year}</b><span>${esc(i.label)}</span></button>`).join('')}</div><p>${esc(selected.summary)}</p><div class="ds-connection"><b>Conexão com DS</b><span>${esc(selected.dsLink)}</span></div><a class="button secondary small" href="${selected.source}" target="_blank" rel="noreferrer">Fonte oficial</a></section></aside></div>`;}
 telescopes(){const snap=this.system.snapshot();return `<div class="observatory-work-grid"><section class="section glass"><div class="section-head"><div><h3>Seleção de instrumento</h3><p>Cada faixa do espectro revela uma parte diferente do mesmo objeto.</p></div><span class="tag">Compatibilidade ${snap.compatibility.score}%</span></div><div class="telescope-grid">${TELESCOPES.map(t=>`<button class="telescope-card ${t.id===snap.telescope.id?'active':''}" data-action="obs-telescope" data-telescope="${t.id}"><span>${t.band==='infrared'?'◈':t.band==='xray'?'✦':t.band==='radio'?'⌁':'◎'}</span><b>${t.label}</b><small>${t.wavelength}</small><em>${t.aperture} m · sensibilidade ${Math.round(t.sensitivity*100)}%</em></button>`).join('')}</div><div class="instrument-console"><label class="field"><span>Alvo</span><select data-obs="target">${TARGETS.map(i=>`<option value="${i.id}" ${i.id===snap.target.id?'selected':''}>${esc(i.label)}</option>`).join('')}</select></label><label class="field"><span>Exposição</span><input type="range" min="5" max="600" value="${this.exposure}" data-obs="exposure"><b id="obs-exposure-label">${this.exposure}s</b></label><div class="instrument-metrics"><article><b>${fmt(snap.trackingError,2)}°</b><span>erro de rastreio</span></article><article><b>${Math.round(snap.focus*100)}%</b><span>foco</span></article><article><b>${snap.compatibility.score}%</b><span>sinal útil</span></article></div><div class="hero-actions"><button class="button secondary" data-action="obs-disturb">Simular perturbação</button><button class="button primary" data-action="obs-align">Alinhar e focar</button></div><p class="validation-note ${snap.compatibility.supported?'ok':'danger'}">${snap.compatibility.reason}</p></div></section><aside class="section glass"><h3>Desafios de instrumento</h3><div class="challenge-stack">${OBSERVATION_CHALLENGES.map(c=>`<article><b>${esc(TARGETS.find(t=>t.id===c.targetId)?.label)}</b><span>Qual instrumento priorizar?</span><div class="mini-options">${TELESCOPES.map(t=>`<button data-action="obs-challenge" data-challenge="${c.id}" data-answer="${t.id}">${t.label}</button>`).join('')}</div></article>`).join('')}</div><p class="feedback-line">${esc(this.challengeFeedback)}</p></aside></div>`;}
 imaging(){return `<div class="image-lab-grid"><section class="section glass"><div class="section-head"><div><h3>Pipeline de imagem científica</h3><p>Raw → calibração → alinhamento → composição → contraste → registro.</p></div><span class="tag" id="obs-processing">Worker aguardando</span></div><div class="science-image-wrap"><canvas id="science-image" width="384" height="384" aria-label="Imagem científica processada"></canvas><div class="image-scanline"></div></div><div class="before-after"><span>Dados sintéticos</span><span>Composição científica</span></div></section><aside class="side-stack"><section class="panel glass"><h3>Aquisição</h3><label class="field"><span>Exposição <b>${this.exposure}s</b></span><input type="range" min="5" max="600" value="${this.exposure}" data-obs="exposure"></label><label class="field"><span>Ruído <b>${Math.round(this.noise*100)}%</b></span><input type="range" min="0" max="45" value="${Math.round(this.noise*100)}" data-obs="noise"></label><label class="toggle-row"><span>Calibração dark/flat</span><input type="checkbox" data-obs="calibration" ${this.calibration?'checked':''}></label><h4>Canais</h4><div class="filter-grid">${FILTERS.slice(0,9).map(f=>`<label class="filter-chip"><input type="checkbox" data-filter="${f.channel}" ${this.filters.has(f.channel)?'checked':''}><span>${f.channel}</span></label>`).join('')}</div><button class="button primary" data-action="obs-process">Processar imagem</button></section><section class="panel glass"><h3>Qualidade do resultado</h3><div class="metrics"><div class="metric"><strong id="obs-snr">--</strong><span>SNR</span></div><div class="metric"><strong id="obs-range">--</strong><span>Faixa dinâmica</span></div><div class="metric"><strong id="obs-pixels">--</strong><span>Pixels</span></div></div><button class="button secondary" data-action="obs-complete-imaging">Validar pipeline · 300 XP</button></section></aside></div>`;}
 spectroscopy(){return `<div class="observatory-work-grid"><section class="section glass"><div class="section-head"><div><h3>Espectroscopia didática</h3><p>As linhas não formam uma fotografia: elas funcionam como assinaturas químicas.</p></div></div><div class="spectrum-stage"><div class="spectrum-rainbow">${Array.from({length:42},(_,i)=>`<i style="left:${i/41*100}%"></i>`).join('')}</div><div class="spectral-lines" id="spectral-lines">${SPECTRUM_CHALLENGES[0].sample.map(v=>`<span style="left:${Math.max(0,Math.min(100,(v-380)/350*100))}%" title="${v} nm"></span>`).join('')}</div></div><div class="spectrum-result" id="spectrum-result"><b>Escolha um desafio e identifique o elemento.</b></div></section><aside class="section glass"><h3>Desafios espectrais</h3>${SPECTRUM_CHALLENGES.map(c=>`<article class="spectrum-challenge"><b>${c.label}</b><small>${c.sample.join(' · ')} nm</small><div class="mini-options">${['hydrogen','helium','oxygen','sodium','calcium'].map(id=>`<button data-action="spectrum-answer" data-challenge="${c.id}" data-answer="${id}">${id}</button>`).join('')}</div></article>`).join('')}<p class="feedback-line">${esc(this.spectrumFeedback)}</p></aside></div>`;}
 database(){const stats=this.db.stats();return `<div class="observatory-work-grid"><section class="section glass"><div class="section-head"><div><h3>Banco de observações</h3><p>Resultados científicos precisam de metadados, proveniência e reprodutibilidade.</p></div><span class="tag">${stats.count} registros</span></div><div class="observation-table"><div class="observation-row head"><span>ID</span><span>Alvo</span><span>Instrumento</span><span>SNR</span></div>${this.db.list().map(r=>`<div class="observation-row"><b>${esc(r.id)}</b><span>${esc(r.targetId)}</span><span>${esc(r.telescopeId)}</span><span>${fmt(r.snr,2)}</span></div>`).join('')||'<p class="empty-state">Nenhuma observação registrada.</p>'}</div></section><aside class="side-stack"><section class="panel glass"><h3>Registrar quadro atual</h3><label class="field"><span>ID</span><input id="obs-record-id" value="OBS-${String(stats.count+1).padStart(3,'0')}"></label><label class="field"><span>Notas</span><textarea id="obs-record-notes" rows="4" placeholder="Condições, hipótese e interpretação"></textarea></label><button class="button primary" data-action="obs-save-record">Salvar observação</button><button class="button secondary" data-action="obs-export">Exportar JSON</button></section><section class="panel glass"><h3>Resumo</h3><div class="metrics"><div class="metric"><strong>${stats.count}</strong><span>registros</span></div><div class="metric"><strong>${fmt(stats.averageSnr,2)}</strong><span>SNR médio</span></div></div></section></aside></div>`;}
 certMarkup(){const required=['observatory-planetarium','observatory-imaging',...OBSERVATION_CHALLENGES.map(c=>c.id),...SPECTRUM_CHALLENGES.map(c=>c.id),'observatory-database'];const done=required.filter(id=>this.context.profileStore.hasCompleted(id)).length;return `<div><span class="eyebrow">Certificação da Fase 9</span><h3>Especialista em Sistemas de Observação</h3><p>${done}/${required.length} checkpoints concluídos. A certificação exige exploração 3D, instrumento, pipeline, espectro e banco.</p></div><button class="button primary" data-action="obs-certify" ${done<required.length||this.certified?'disabled':''}>${this.certified?'Certificação emitida':'Emitir certificação · 450 XP'}</button>`;}
 afterScene(){const canvas=this.container.querySelector('#universe-scene');if(!canvas)return;this.renderer=new UniverseSceneRenderer(canvas,this.context.settingsStore,{onDiagnostics:d=>{this.diagnostics=d;const el=this.container?.querySelector('#universe-diagnostics');if(el)el.innerHTML=`<span>${d.renderer}</span><span>${d.resolution}</span><span>${d.profile}</span><span>${d.mode}</span><span>zoom ${d.zoom}</span>`;const zoom=this.container?.querySelector('#obs-zoom');if(zoom)zoom.textContent=`${d.zoom.toFixed(2).replace('.',',')}×`;}});this.renderer.setMode(this.system.target().scene);this.renderer.start();}
 drawFrame(){const canvas=this.container?.querySelector('#science-image');if(!canvas||!this.frame)return;const ctx=canvas.getContext('2d'),rgba=this.pipeline.toRgba(this.frame,{contrast:this.contrast,gamma:this.gamma}),image=new ImageData(rgba,this.frame.size,this.frame.size),tmp=document.createElement('canvas');tmp.width=this.frame.size;tmp.height=this.frame.size;tmp.getContext('2d').putImageData(image,0,0);ctx.imageSmoothingEnabled=true;ctx.clearRect(0,0,canvas.width,canvas.height);ctx.drawImage(tmp,0,0,canvas.width,canvas.height);}
 updateStats(){if(!this.frame)return;const $=s=>this.container?.querySelector(s);if($('#obs-snr'))$('#obs-snr').textContent=this.frame.stats.snr;if($('#obs-range'))$('#obs-range').textContent=this.frame.stats.dynamicRange;if($('#obs-pixels'))$('#obs-pixels').textContent=this.frame.stats.pixels;if($('#obs-processing'))$('#obs-processing').textContent=`${this.frame.processingMs} ms · ${this.frame.quality}`;}
 award(xp,id,msg){if(this.context.profileStore.addXp(xp,id))this.context.toast(`${msg} +${xp} XP`);else this.context.toast('Checkpoint já registrado para este perfil.');this.render();}
 click(e){const t=e.target.closest('[data-action]');if(!t)return;const a=t.dataset.action;if(a==='obs-tab'){this.tab=t.dataset.tab;this.render();}if(a==='obs-era'){this.timelineId=t.dataset.era;this.render();}if(a==='obs-mode'){this.renderer?.setMode(t.dataset.mode);}if(a==='obs-fullscreen'){this.container.querySelector('.universe-scene-wrap')?.requestFullscreen?.();}if(a==='obs-complete-planetarium')this.award(240,'observatory-planetarium','Exploração do universo registrada.');if(a==='obs-telescope'){this.system.configure({telescopeId:t.dataset.telescope});this.render();}if(a==='obs-align'){this.system.align();this.render();}if(a==='obs-disturb'){this.system.disturb();this.render();}if(a==='obs-challenge'){const c=OBSERVATION_CHALLENGES.find(i=>i.id===t.dataset.challenge);if(!c)return;if(t.dataset.answer===c.telescopeId){this.challengeFeedback=`Correto: ${c.reason}`;this.award(c.xp,c.id,'Decisão instrumental validada.');}else{this.challengeFeedback='Instrumento inadequado para a emissão prioritária deste alvo.';this.render();}}if(a==='obs-process')this.process();if(a==='obs-complete-imaging'){if(!this.frame||this.frame.stats.snr<1.2)this.context.toast('Aumente a relação sinal/ruído antes de validar.');else this.award(300,'observatory-imaging','Pipeline científico validado.');}if(a==='spectrum-answer'){const c=SPECTRUM_CHALLENGES.find(i=>i.id===t.dataset.challenge);const result=this.spectrum.identify(c.sample);if(t.dataset.answer===c.answer){this.spectrumFeedback=`Correto: ${result.best.label}, confiança didática ${result.best.score}%.`;this.award(c.xp,c.id,'Assinatura espectral identificada.');}else{this.spectrumFeedback=`A melhor correspondência calculada foi ${result.best.label}.`;this.render();}}if(a==='obs-save-record'){if(!this.frame){this.context.toast('Processe uma imagem antes de registrar.');return;}const id=this.container.querySelector('#obs-record-id')?.value,notes=this.container.querySelector('#obs-record-notes')?.value,s=this.system.snapshot();const res=this.db.add({id,targetId:s.target.id,telescopeId:s.telescope.id,filters:[...this.filters],exposure:this.exposure,snr:this.frame.stats.snr,notes});if(res.ok){this.save();if(this.db.stats().count>=1)this.context.profileStore.addXp(260,'observatory-database');this.context.toast('Observação registrada com proveniência.');this.render();}else this.context.toast(res.reason);}if(a==='obs-export'){const blob=new Blob([this.db.export()],{type:'application/json'}),url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download='cosmos-ds-observacoes.json';link.click();setTimeout(()=>URL.revokeObjectURL(url),500);}if(a==='obs-certify'){this.certified=true;this.award(450,'observatory-certification','Certificação da Fase 9 emitida.');}}
 change(e){const el=e.target;if(el.matches('[data-obs="target"]')){this.system.configure({targetId:el.value});this.render();}if(el.matches('[data-obs="exposure"]')){this.exposure=Number(el.value);if(this.tab==='imaging')this.process();else this.render();}if(el.matches('[data-obs="noise"]')){this.noise=Number(el.value)/100;this.process();}if(el.matches('[data-obs="calibration"]')){this.calibration=el.checked;this.process();}if(el.matches('[data-filter]')){el.checked?this.filters.add(el.dataset.filter):this.filters.delete(el.dataset.filter);if(!this.filters.size){this.filters.add('L');this.context.toast('Ao menos um canal deve permanecer ativo.');}this.process();}}
}
export const createModule=()=>new ObservatoryModule();
