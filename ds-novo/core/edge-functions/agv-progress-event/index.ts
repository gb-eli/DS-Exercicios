import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "jsr:@supabase/supabase-js@2";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS"};
const json=(b:unknown,s=200)=>new Response(JSON.stringify(b),{status:s,headers:{...cors,"content-type":"application/json"}});
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(req.method!=='POST')return json({error:'method_not_allowed'},405);try{const auth=req.headers.get('Authorization')||'';const url=Deno.env.get('SUPABASE_URL')!;const anon=Deno.env.get('SUPABASE_ANON_KEY')!;const service=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;const userClient=createClient(url,anon,{global:{headers:{Authorization:auth}}});const {data:{user},error:ue}=await userClient.auth.getUser();if(ue||!user)return json({error:'unauthorized'},401);const admin=createClient(url,service,{auth:{persistSession:false,autoRefreshToken:false}});const body=await req.json();const {data,error}=await admin.rpc('record_core_progress_event',{p_user_id:user.id,p_platform_code:String(body.platformId||''),p_activity_id:body.activityId?String(body.activityId):null,p_event_type:String(body.eventType||''),p_progress:body.progress??null,p_score:body.score??null,p_payload:body.payload&&typeof body.payload==='object'?body.payload:{},p_idempotency_key:String(body.idempotencyKey||''),p_occurred_at:body.occurredAt||new Date().toISOString()});if(error)return json({error:'progress_rejected',message:error.message},400);return json(data||{ok:true});}catch(error){return json({error:'invalid_request',message:String(error?.message||error)},400)}});
