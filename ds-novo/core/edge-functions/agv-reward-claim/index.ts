import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "jsr:@supabase/supabase-js@2";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS"};
const json=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:{...cors,"content-type":"application/json"}});
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(req.method!=='POST')return json({error:'method_not_allowed'},405);try{const auth=req.headers.get('Authorization')||'';const client=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_ANON_KEY')!,{global:{headers:{Authorization:auth}}});const body=await req.json();if(Object.prototype.hasOwnProperty.call(body,'amount'))return json({error:'client_amount_forbidden',message:'O valor oficial da recompensa é calculado pelo Core.'},400);const {data,error}=await client.rpc('claim_core_reward',{p_platform_code:String(body.platformId||''),p_activity_id:body.activityId?String(body.activityId):null,p_event_type:String(body.eventType||''),p_evidence_id:body.evidenceId?String(body.evidenceId):null,p_attempt_id:body.attemptId?String(body.attemptId):null,p_metadata:body.metadata&&typeof body.metadata==='object'?body.metadata:{},p_idempotency_key:String(body.idempotencyKey||'')});if(error)return json({error:'reward_rejected',message:error.message},400);return json(data||{ok:true});}catch(error){return json({error:'invalid_request',message:String(error?.message||error)},400)}});
