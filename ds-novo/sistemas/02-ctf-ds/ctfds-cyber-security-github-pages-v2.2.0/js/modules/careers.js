import { careers, educationPaths, hiringSectors } from '../data/careers.js';
import { escapeHtml } from '../core/utils.js';

export const renderCareers = (profile) => `
  <div class="page-head"><div><p class="eyebrow">CARREIRAS // ROTAS PROFISSIONAIS</p><h1>Cyber Career Hub</h1><p>Explore cargos, competências, caminhos de formação e setores que mantêm equipes de segurança. Os nomes variam entre empresas; compare sempre as responsabilidades da vaga.</p></div>${profile.careerGoal ? `<span class="resource-pill">ROTA: ${escapeHtml(careers.find((item) => item.id === profile.careerGoal)?.title || profile.careerGoal)}</span>` : ''}</div>
  <div class="section-title"><h2>CARGOS E ESPECIALIZAÇÕES</h2><small>ESCOLHA UMA ROTA DE INTERESSE</small></div>
  <div class="career-grid">${careers.map((career) => `<article class="career-card card ${profile.careerGoal === career.id ? 'selected' : ''}"><div class="career-icon">${career.icon}</div><p class="eyebrow">${escapeHtml(career.level)}</p><h3>${escapeHtml(career.title)}</h3><p>${escapeHtml(career.mission)}</p><div class="tag-list">${career.skills.map((skill) => `<span>${escapeHtml(skill)}</span>`).join('')}</div><div class="career-route"><strong>ROTA SUGERIDA</strong><p>${escapeHtml(career.route)}</p></div><button class="${profile.careerGoal === career.id ? 'secondary-button' : 'primary-button'} full" data-career-goal="${career.id}">${profile.careerGoal === career.id ? 'ROTA SELECIONADA' : 'DEFINIR COMO OBJETIVO'}</button></article>`).join('')}</div>
  <div class="section-title"><h2>FORMAÇÃO ACADÊMICA</h2><small>NÃO EXISTE UMA ÚNICA PORTA DE ENTRADA</small></div>
  <div class="grid grid-4">${educationPaths.map((path) => `<article class="card"><h3>${escapeHtml(path.title)}</h3><p>${escapeHtml(path.text)}</p></article>`).join('')}</div>
  <div class="section-title"><h2>SETORES CONTRATANTES</h2><small>DEMANDA DISTRIBUÍDA EM TODA A ECONOMIA</small></div>
  <div class="grid grid-3">${hiringSectors.map((sector) => `<article class="card"><div class="track-icon">${sector.icon}</div><h3>${escapeHtml(sector.title)}</h3><p>${escapeHtml(sector.text)}</p></article>`).join('')}</div>
  <div class="section-title"><h2>TRILHA DE PREPARAÇÃO</h2><small>PORTFÓLIO É EVIDÊNCIA DE APRENDIZADO</small></div>
  <article class="card"><p style="margin:0">Construa fundamentos de redes, sistemas operacionais e programação; pratique apenas em laboratórios autorizados; documente relatórios, diagramas e correções; desenvolva comunicação; estude inglês técnico; e mantenha um portfólio sem dados sensíveis. Certificações podem complementar, mas não substituem prática, ética e fundamentos.</p></article>`;
