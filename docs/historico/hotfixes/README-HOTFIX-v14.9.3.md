# HOTFIX v14.9.3 — DS2 HTML/CSS/JS separados

Backend e referências do Supabase já foram corrigidos em produção.

Para atualizar a hospedagem estática, copie os arquivos deste ZIP preservando os mesmos caminhos.
O hotfix inclui o fallback canônico do DS2, workspace reconciliador e cache-busting v14.9.3.

Não publique o diretório `core/` no site estático; migrations e Edge Functions já foram aplicadas no Supabase.
